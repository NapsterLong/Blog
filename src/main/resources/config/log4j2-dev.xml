<?xml version="1.0" encoding="UTF-8"?>
<configuration status="off">
    <properties>
        <property name="PROJECT_NAME">Blog</property>
        <property name="LOG_PATH">/logs</property>
        <property name="LOG_BACK_PATH">${LOG_PATH}/history</property>
        <property name="ERROR_FILE_NAME">error.log</property>
        <property name="WARN_FILE_NAME">warn.log</property>
        <property name="INFO_FILE_NAME">info.log</property>
        <property name="CONFIG_FILE_NAME">config.log</property>
    </properties>
    <Appenders>
        <!--Console log-->
        <Console name="console" target="SYSTEM_OUT">
            <!--控制台只输出level及以上级别的信息（onMatch），其他的直接拒绝（onMismatch）-->
            <ThresholdFilter level="info" onMatch="ACCEPT" onMismatch="DENY"/>
            <!--这个都知道是输出日志的格式-->
            <PatternLayout pattern="%date{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] [%file:%line] - %msg%n"/>
        </Console>
        <!-- Error log -->
        <RollingRandomAccessFile name="error" fileName="${LOG_PATH}/${PROJECT_NAME}/${ERROR_FILE_NAME}"
                                 filePattern="${LOG_BACK_PATH}/${date:yyyy-MM}/${ERROR_FILE_NAME}.%d{yyyy-MM-dd}.log"
                                 append="true">
            <!-- 输出格式 -->
            <PatternLayout pattern="%date{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] - %msg%n"/>
            <!-- 设置策略 -->
            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
            </Policies>
            <Filters>
                <ThresholdFilter level="error" onMatch="ACCEPT" onMismatch="DENY"/>
            </Filters>
        </RollingRandomAccessFile>
        <!-- Warn log -->
        <RollingRandomAccessFile name="warn" fileName="${LOG_PATH}/${PROJECT_NAME}/${WARN_FILE_NAME}"
                                 filePattern="${LOG_BACK_PATH}/$${date:yyyy-MM}/${WARN_FILE_NAME}.%d{yyyy-MM-dd}.log"
                                 append="true">
            <!-- 输出格式 -->
            <PatternLayout pattern="%date{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread]  - %msg%n"/>
            <!-- 设置策略 -->
            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
            </Policies>
            <Filters>
                <ThresholdFilter level="error" onMatch="DENY" onMismatch="NEUTRAL"/>
                <ThresholdFilter level="warn" onMatch="ACCEPT" onMismatch="DENY"/>
            </Filters>
        </RollingRandomAccessFile>
        <!-- Info log -->
        <RollingRandomAccessFile name="info" fileName="${LOG_PATH}/${PROJECT_NAME}/${INFO_FILE_NAME}"
                                 filePattern="${LOG_BACK_PATH}/$${date:yyyy-MM}/${INFO_FILE_NAME}.%d{yyyy-MM-dd}.log"
                                 append="true">
            <!-- 输出格式 -->
            <PatternLayout pattern="%date{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] - %msg%n"/>
            <!-- 设置策略 -->
            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
            </Policies>
            <Filters>
                <ThresholdFilter level="warn" onMatch="DENY" onMismatch="NEUTRAL"/>
                <ThresholdFilter level="info" onMatch="ACCEPT" onMismatch="DENY"/>
            </Filters>
        </RollingRandomAccessFile>
        <!-- Config log -->
        <RollingRandomAccessFile name="config" fileName="${LOG_PATH}/${PROJECT_NAME}/${CONFIG_FILE_NAME}"
                                 filePattern="${LOG_BACK_PATH}/$${date:yyyy-MM}/${CONFIG_FILE_NAME}.%d{yyyy-MM-dd}.log"
                                 append="true">
            <!-- 输出格式 -->
            <PatternLayout pattern="%date{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] - %msg%n"/>
            <!-- 设置策略 -->
            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
            </Policies>
            <Filters>
                <ThresholdFilter level="info" onMatch="ACCEPT" onMismatch="ACCEPT"/>
            </Filters>
        </RollingRandomAccessFile>

        <!--Async -->
        <Async name="asyncError">
            <AppenderRef ref="error"/>
        </Async>
        <Async name="asyncWarn">
            <AppenderRef ref="warn"/>
        </Async>
        <Async name="asyncInfo">
            <AppenderRef ref="info"/>
        </Async>
        <Async name="asyncConfig">
            <AppenderRef ref="config"/>
        </Async>
    </Appenders>
    <Loggers>
        <Root level="info">
            <AppenderRef ref="console"/>
            <AppenderRef ref="asyncConfig"/>
        </Root>
        <Logger name="info" additivity="true">
            <AppenderRef ref="asyncInfo"/>
        </Logger>
        <Logger name="warn" additivity="true">
            <AppenderRef ref="asyncWarn"/>
        </Logger>
        <Logger name="error" additivity="true">
            <AppenderRef ref="asyncError"/>
        </Logger>
    </Loggers>
</configuration>
